@go.Package { name = "github.com/Mind2Screen-Dev-Team/go-skeleton/gen/pkl/natsconfig" }
module NatsConfig

import "package://pkg.pkl-lang.org/pkl-go/pkl.golang@0.5.0#/go.pkl"

class Auth {
  /// Nats authorization is nabled
  enabled: Boolean = true

  /// Nats authorization username
  username: String(!isEmpty) = "mysql"

  /// Nats authorization password
  password: String(!isEmpty) = "secret"
}

/// Nats Feature is enabled
enabled: Boolean = true

/// The hostname that Nats listens on
host: String(!isEmpty) = "localhost"

/// The port that Nats listens on
port: Int = 4222

/// Authorization settings for Nats
auth: Auth