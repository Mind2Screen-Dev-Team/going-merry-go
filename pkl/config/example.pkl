amends "../AppConfig.pkl"

app {
  name = "Go-Skeleton-App"
  domain = "localhost"
  jwt {
    secret = "gdSyjIkTk47yhSH1RCSlQVmrhmOrzdXi"
  }
  log {
    path = "./storage/logs"
    maxBackups = 10
    maxSize = 100
    maxAge = 0
    timeFormat = "UnixMs"
    consoleLoggingEnabled = true
    fileLoggingEnabled = true
    localTime = false
    compress = false
  }
  http {
    service = "go-skeleton-app-rest-api"
    host = "127.0.0.1"
    port = 8080
    idleTimeout = 30
    readHeaderTimeout = 5
    readTimeout = 15
    writeTimeout = 20
    handlerTimeout = 60
  }
}
mysql {
  enabled = true
  host = "localhost"
  port = 3306
  db = "skeleton-db"
  auth {
    enabled = true
    username = "mysql"
    password = "secret"
  }
}
redis {
  enabled = true
  host = "localhost"
  port = 6379
  db = 0
  auth {
    enabled = true
    username = "mysql"
    password = "secret"
  }
}
nats {
  enabled = true
  host = "localhost"
  port = 4222
  auth {
    enabled = true
    username = "mysql"
    password = "secret"
  }
}
