amends "../AppConfig.pkl"

appName = "Go-Skeleton"
appDomain = "localhost"
appHost = "127.0.0.1"
appHttpPort = 1234
appJwtSecret = ""
appLogPath = "../storage/logs"

redis {
  enabled = true
  host = "localhost"
  port = 5678
  auth {
    username = "redis"
    password = "redis"
  }
}

nats {
  enabled = true
  host = "localhost"
  port = 5678
  auth {
    username = "redis"
    password = "redis"
  }
}

mysql {
  enabled = true
  host = "localhost"
  port = 5678
  db = "test"
  auth {
    username = "redis"
    password = "redis"
  }
}