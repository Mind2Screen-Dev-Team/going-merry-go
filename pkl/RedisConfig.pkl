@go.Package { name = "github.com/Mind2Screen-Dev-Team/go-skeleton/gen/pkl/redisconfig" }
module RedisConfig

import "package://pkg.pkl-lang.org/pkl-go/pkl.golang@0.5.0#/go.pkl"

class Auth {
  /// Authorization Enabled
  enabled: Boolean = true

  /// Authorization username
  username: String = "default"

  /// Authorization password
  password: String = "secret"
}

/// Redis Feature is enabled
enabled: Boolean = true

/// The hostname that Redis listens on
host: String(!isEmpty) = "localhost"

/// The port that Redis listens on
port: Int = 6379

/// The Redis DB selection
db: Int = 0

/// Authorization settings for Redis
auth: Auth